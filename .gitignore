# nbb dependency cache (regenerated from nbb.edn; needs bb on a cold machine).
# Ignored because this repository has no cold-cache-without-bb path: it is in
# no fleet-ci gate and has no .github/workflows (measured 2026-09-11).
.nbb/
